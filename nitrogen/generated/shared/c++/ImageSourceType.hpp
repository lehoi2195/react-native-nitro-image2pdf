///
/// ImageSourceType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroimage2pdf {

  /**
   * An enum which can be represented as a JavaScript union (ImageSourceType).
   */
  enum class ImageSourceType {
    PATH      SWIFT_NAME(path) = 0,
    URI      SWIFT_NAME(uri) = 1,
    BASE64      SWIFT_NAME(base64) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroimage2pdf

namespace margelo::nitro {

  // C++ ImageSourceType <> JS ImageSourceType (union)
  template <>
  struct JSIConverter<margelo::nitro::nitroimage2pdf::ImageSourceType> final {
    static inline margelo::nitro::nitroimage2pdf::ImageSourceType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("path"): return margelo::nitro::nitroimage2pdf::ImageSourceType::PATH;
        case hashString("uri"): return margelo::nitro::nitroimage2pdf::ImageSourceType::URI;
        case hashString("base64"): return margelo::nitro::nitroimage2pdf::ImageSourceType::BASE64;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ImageSourceType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitroimage2pdf::ImageSourceType arg) {
      switch (arg) {
        case margelo::nitro::nitroimage2pdf::ImageSourceType::PATH: return JSIConverter<std::string>::toJSI(runtime, "path");
        case margelo::nitro::nitroimage2pdf::ImageSourceType::URI: return JSIConverter<std::string>::toJSI(runtime, "uri");
        case margelo::nitro::nitroimage2pdf::ImageSourceType::BASE64: return JSIConverter<std::string>::toJSI(runtime, "base64");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ImageSourceType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("path"):
        case hashString("uri"):
        case hashString("base64"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
