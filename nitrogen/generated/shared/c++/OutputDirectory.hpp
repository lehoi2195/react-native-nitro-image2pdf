///
/// OutputDirectory.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroimage2pdf {

  /**
   * An enum which can be represented as a JavaScript union (OutputDirectory).
   */
  enum class OutputDirectory {
    CACHE      SWIFT_NAME(cache) = 0,
    DOCUMENTS      SWIFT_NAME(documents) = 1,
    TEMP      SWIFT_NAME(temp) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroimage2pdf

namespace margelo::nitro {

  // C++ OutputDirectory <> JS OutputDirectory (union)
  template <>
  struct JSIConverter<margelo::nitro::nitroimage2pdf::OutputDirectory> final {
    static inline margelo::nitro::nitroimage2pdf::OutputDirectory fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("cache"): return margelo::nitro::nitroimage2pdf::OutputDirectory::CACHE;
        case hashString("documents"): return margelo::nitro::nitroimage2pdf::OutputDirectory::DOCUMENTS;
        case hashString("temp"): return margelo::nitro::nitroimage2pdf::OutputDirectory::TEMP;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum OutputDirectory - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitroimage2pdf::OutputDirectory arg) {
      switch (arg) {
        case margelo::nitro::nitroimage2pdf::OutputDirectory::CACHE: return JSIConverter<std::string>::toJSI(runtime, "cache");
        case margelo::nitro::nitroimage2pdf::OutputDirectory::DOCUMENTS: return JSIConverter<std::string>::toJSI(runtime, "documents");
        case margelo::nitro::nitroimage2pdf::OutputDirectory::TEMP: return JSIConverter<std::string>::toJSI(runtime, "temp");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert OutputDirectory to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("cache"):
        case hashString("documents"):
        case hashString("temp"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
