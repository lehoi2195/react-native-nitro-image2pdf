///
/// SecurityConfig.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `SecurityConfig`, backed by a C++ struct.
 */
public typealias SecurityConfig = margelo.nitro.nitroimage2pdf.SecurityConfig

public extension SecurityConfig {
  private typealias bridge = margelo.nitro.nitroimage2pdf.bridge.swift

  /**
   * Create a new instance of `SecurityConfig`.
   */
  init(password: String?, ownerPassword: String?) {
    self.init({ () -> bridge.std__optional_std__string_ in
      if let __unwrappedValue = password {
        return bridge.create_std__optional_std__string_(std.string(__unwrappedValue))
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_std__string_ in
      if let __unwrappedValue = ownerPassword {
        return bridge.create_std__optional_std__string_(std.string(__unwrappedValue))
      } else {
        return .init()
      }
    }())
  }

  @inline(__always)
  var password: String? {
    return { () -> String? in
      if bridge.has_value_std__optional_std__string_(self.__password) {
        let __unwrapped = bridge.get_std__optional_std__string_(self.__password)
        return String(__unwrapped)
      } else {
        return nil
      }
    }()
  }
  
  @inline(__always)
  var ownerPassword: String? {
    return { () -> String? in
      if bridge.has_value_std__optional_std__string_(self.__ownerPassword) {
        let __unwrapped = bridge.get_std__optional_std__string_(self.__ownerPassword)
        return String(__unwrapped)
      } else {
        return nil
      }
    }()
  }
}
